Функциональные (что делает система):

1 Регистрация и аутентификация пользователей (роли: клиент, доктор, администратор)
2 Управление профилями докторов (CRUD)
3 Запись на приём (создание расписания клиентом)
4 Подтверждение/отмена записей доктором
5 Загрузка документов к записи (рецепты, анализы)
6 Просмотр истории посещений для клиента
7 Управление питомцами (добавление, редактирование)
8 Фильтрация докторов по специализации
9 Экспорт расписания доктора в PDF
10 Уведомления о предстоящих записях (email)

Нефункциональные (как делает система):

1 Производительность: Время отклика API < 200 мс
2 Безопасность: Хеширование паролей (bcrypt), защита от SQL-инъекций, CSRF -токены
3 Масштабируемость: Поддержка 1000+ одновременных пользователей
4 Удобство использования: Адаптивный интерфейс, поддержка мобильных устройств
5 Совместимость: Поддержка браузеров Chrome 80+, Firefox 75+, Safari 13+
6 Сопровождаемость: Соблюдение PSR-12, документация кода
7 Надёжность: Резервное копирование БД ежедневно, uptime 99.5%
8 Локализация: Поддержка русского и английского языков


<?php
namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\Models\Doctor;

class DoctorController extends Controller
{
    public function index()
    {
        $doctors = Doctor::all(); // данная строка выполняет выборку всех врачей из базы данных
        return view('doctors.index', compact('doctors')); // возврашает нам всеX врачей
    }

    public function create()
    {
        return view('doctors.create'); // nokазывает оорму создания врачей
    }

    public function store (Request $request)
    {
        $doctor = Doctor::create($request->all()); // создаем врача по данным которые мы получаем из формы
        return redirect('/doctors')->with('success', 'Врач добавлен'); // перенаправляем на страницу врачей
    }

    public function show(string $id) // для показа 1 конкретного врача
    {
        $doctor Doctor::findOrFail($id); // ишем врача конкретно по id
        return view('doctors.show', compact('doctor')); // возврашаем его в случае успеxа в þaйлe show
    }

    public function edit(string $id)
    {
        $doctor = Doctor::findOrFail($id); // ишем врача конкретно пo id
        return view('doctors.edit', compact('doctor')); // возврашаем его в случае успеxa
    }

    public function update (Request $request, string $id)
    {
        $doctor = Doctor::findOrFail($id); // ишем врача конкретнO no id
        $doctor->update($request->all()); // обновляем его, если поле без изменений старые данные не изменять return redirect("/doctors/{$doctor->id}")->with('success', 'Данные обновлены'); // перенаправляем на
    }

    public function destroy(string $id) // удаляем врачей
    {
        $doctor = Doctor::findOrFail($id); // ишем врача конкретнO no id
        $doctor->delete(); // удаляем
        return redirect('/doctors')->with('success', 'Bpaч удален'); // перенаправляем на страничу врачей
    }
}

-------------------------------------------

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Relations\HasMany;
use Illuminate\Foundation\Auth\User as Authenticatable;


class User extends Authenticatable
{

    protected $fillable = [
        'name',
        'email',
        'password',
    ];


    protected $hidden = [
        'password',
        'remember_token',
    ];


    protected function casts(): array
    {
        return [
            'email_verified_at' => 'datetime',
            'password' => 'hashed',
        ];
    }

    public function tasks(): HasMany
    {
        return $this -> hasMany(Task::class);
    }
}

------------------------------

<?php

namespace App\Http\Controllers;

use App\Models\User;
use Illuminate\Http\RedirectResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Auth;

class UserController extends Controller
{
    public function register(Request $request): RedirectResponse
    {
        // Validate the request data
        $request->validate([
            'name' => 'required|string|max:255',
            'email' => 'required|string|email|max:255|unique:users',
            'password' => 'required|string|min:8',
        ]);

        // Create a new user instance
        User::create([
            'name' => $request->name,
            'email' => $request->email,
            'password' => bcrypt($request->password), // Hash the password
        ]);

        return redirect()
            -> route('index');
    }

    public function login(Request $request): RedirectResponse
    {
        // Validate the request data
        $request->validate([
            'email' => 'required|string|email',
            'password' => 'required|string',
        ]);
        // Attempt to log the user in
        if (Auth::attempt($request->only('email', 'password'))) {
            return redirect()
                -> route('index');
        }

        return redirect()
            -> back();
    }
    /**
     * Завершает сеанс аутентификации пользователя и перенаправляет на главную страницу.
     *
     * @return \Illuminate\Http\RedirectResponse Ответ с перенаправлением на маршрут 'index'.
     */
    public function logout(): RedirectResponse
    {
        Auth::logout();

        return redirect()
            -> route('index');
    }
}



<?php

namespace App\Http\Controllers;

use App\Models\Task;
use Illuminate\Contracts\View\View;
use Illuminate\Http\RedirectResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Auth;

class TaskController extends Controller
{

    public function index(?Task $task = null): View
    {
        return view('task', [
            'task' => $task,
        ]);
    }


    public function createOrUpdate(Request $request): RedirectResponse
    {
        $request -> validate([
            'title' => 'required|string|max:255',
            'description' => 'required|string',
            'status' => 'required|in:pending,in_progress,completed',
        ]);

        $task = Task::updateOrCreate(
            ['id' => $request -> route('task')],
            [
                'title' => $request -> title,
                'description' => $request -> description,
                'status' => $request -> status,
                'user_id' => Auth::id(),
            ]
        );

        return redirect()
            -> route('tasks.index', ['task' => $task]);
    }

    public function delete(Task $task): RedirectResponse
    {
        $task -> delete();

        return redirect()
            -> route('index');
    }
}



-------------------------------

<?php

use App\Http\Controllers\TaskController;
use App\Http\Controllers\UserController;
use Illuminate\Support\Facades\Route;

Route::get('/', function() {
    return view('index');
}) -> name('index');

Route::get('/register', function() {
    return view('auth.register');
}) -> name('register');
Route::post('/register', [UserController::class, 'register']);

Route::get('/login', function() {
    return view('auth.login');
}) -> name('login');
Route::post('/login', [UserController::class, 'login']);

Route::get('/logout', [UserController::class, 'logout'])
    -> name('logout')
    -> middleware('auth');

Route::get('/tasks/{task?}', [TaskController::class, 'index'])
    -> name('tasks.index')
    -> middleware('auth');
Route::post('/tasks/{task?}', [TaskController::class, 'createOrUpdate'])
    -> middleware('auth');

Route::get('/tasks/delete/{task}', [TaskController::class, 'delete'])
    -> name('tasks.delete')
    -> middleware('auth');


// Ресурсные маршруты (CRUD)
Route::resource('doctors', DoctorController::class);

// С группировкой и префиксами
Route::prefix('admin')->middleware('auth')->group(function () {
    Route::resource('doctors', DoctorController::class);
    Route::resource('schedules', ScheduleController::class);
});

// API-маршруты (без сессий, с токенами)
Route::apiResource('v1/doctors', Api\DoctorController::class);

// Кастомные методы в ресурсном контроллере
Route::get('doctors/{doctor}/export', [DoctorController::class, 'export'])
    ->name('doctors.export');

---------------------------------

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Библиотечный медиацентр НТТЭК</title>
</head>
<body>
    <header>
        <div>
            <nav class="nav-menu">
                <a href="{{ route('home') }}">Главная</a>
                <a href="{{ route('items.books.index') }}">Книги</a>
                <a href="{{ route('items.authors.index') }}">Авторы</a>
                <a href="{{ route('items.publishings.index') }}" >Издательства</a>
                <a href="{{ route('items.types-of-books.index') }}" >Типы книг</a>
                <a href="{{ route('readers.groups.index') }}" >Группы</a>
                <a href="{{ route('readers.readers.index') }}">Читатели</a>
                <a href="{{ route('accounting.issuance.index') }}" >Выдача</a>
                <a href="{{ route('accounting.return.index') }}">Возврат</a>
            </nav>
        </div>
    </header>
    <main>
        @yield('content')
    </main>
</body>
</html>

-------------------------

@extends('layouts.app')

@section('content')
<div>
    <div>
        <h1>Добавление автора</h1>

        <form action="{{ route('items.authors.store') }}" method="POST">
            @csrf

            <div>
                <label for="lastname">Фамилия:</label>
                <input type="text" name="lastname" id="lastname" required>
            </div>

            <div class="form-group">
                <label for="firstname" >Имя:</label>
                <input type="text" name="firstname" id="firstname" required maxlength="100">
            </div>

            <div class="form-group">
                <label for="patronymic">Отчество:</label>
                <input type="text" name="patronymic" id="patronymic">
            </div>

            <div >
                <button type="submit">Сохранить</button>
                <a href="{{ route('items.authors.index') }}">Отмена</a>
            </div>
        </form>
    </div>
</div>

<footer>
    <div>
        <p>&copy; {{ date('Y') }} Библиотечный медиацентр НТТЭК</p>
    </div>
</footer>
@endsection


<body>
    <h1>Register new user</h1>
    <form action="{{ route('register') }}" method="POST">
        @csrf
        <div>
            <label for="name">Name</label>
            <input type="text" name="name" id="name" required>
        </div>
        <div>
            <label for="email">Email</label>
            <input type="email" name="email" id="email" required>
        </div>
        <div>
            <label for="password">Password</label>
            <input type="password" name="password" id="password" required>
        </div>
        <button type="submit">Register</button>
    </form>
    <div>
        <a href="{{ route('login') }}">Already have an account? Login here</a>
    </div>
</body>


<body>
    <h1>Login</h1>
    <form action="{{ route('login') }}" method="POST">
        @csrf
        <div>
            <label for="email">Email</label>
            <input type="email" name="email" id="email" required>
        </div>
        <div>
            <label for="password">Password</label>
            <input type="password" name="password" id="password" required>
        </div>
        <button type="submit">Login</button>
    </form>
    <div>
        <a href="{{ route('register') }}">Don't have an account? Register here</a>
    </div>
</body>


<body>
    <h1>{{ $task ?-> title ?? 'New task' }}</h1>
    <p>Welcome to the task manager application!</p>
    <form action="{{ route('tasks.index', ['task' => $task]) }}" method="POST">
        @csrf
        <label for="title">Title:</label>
        <input type="text" name="title" id="title" value="{{ $task ?-> title }}" required>
        <br><br>
        <label for="description">Description:</label>
        <textarea name="description" id="description" required>{{ $task ?-> description }}</textarea>
        <br><br>
        <label for="status">Status:</label>
        <select name="status" id="status">
            <option value="pending" {{ $task && $task->status == 'pending' ? 'selected' : '' }}>Pending</option>
            <option value="in_progress" {{ $task && $task->status == 'pending' ? 'selected' : '' }}>In progress</option>
            <option value="completed" {{ $task && $task->status == 'completed' ? 'selected' : '' }}>Completed</option>
        </select>
        <br><br>
        <button type="submit">{{ $task ? 'Update' : 'Create' }} task</button>
        <br><br>
        <a href="{{ route('index') }}">Back to tasks</a>
</body>



